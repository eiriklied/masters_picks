<% @users.each do |user| %>
	<div class="score_box">
		<h2><%= user.name %> - <%= user.score %> poeng</h2>
		<ul>
		<% user.players.each do |player| %>
			<li><%= player.position %>. <%= player.name %> - <%= player.score %> poeng</li>
		<% end %>
		</ul>
	</div>
<% end %>
